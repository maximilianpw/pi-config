{
  "name": "maxpw-pi-config",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "extensions/*"
  ],
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "test": "node --import tsx --test test/**/*.test.ts",
    "check": "bun run typecheck && bun run test && bun run --filter pi-save-md-extension check && bun run --filter pi-web-tools-extension check && bun run --filter pi-ask-user-extension check && bun run --filter pi-linear-extension check && bun run --filter pi-skill-toggle check && bun run --filter pi-codex-fast-variants-extension check && bun run --filter pi-subagents-extension check && bun run --filter pi-workflows-extension check"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.84.1",
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@earendil-works/pi-tui": "^0.84.1",
    "typebox": "^1.3.6"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.20.5",
    "typescript": "^7.0.2"
  }
}

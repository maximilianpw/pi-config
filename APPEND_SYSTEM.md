# Pi-specific system prompt additions

Follow the composed Pi `AGENTS.md` loaded from `~/.pi/agent/AGENTS.md`, including the Pi-specific section appended after the shared cross-agent policy.

When a dedicated Pi extension tool is active for a task, prefer that tool over an equivalent shell/CLI fallback unless the tool is insufficient for the requested operation.
